# Git Practice Project

## Steps to practice:

### 1. Initialize
git init
git add .
git commit -m "first commit"

### 2. Create branch
git checkout -b feature/cart

### 3. Make changes, commit
git add .
git commit -m "cart banaya"

### 4. Merge
git checkout main
git merge feature/cart
